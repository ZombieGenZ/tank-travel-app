<Window x:Class="TiketManagementV2.View.ImageGalleryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        mc:Ignorable="d"
        Title="ImageGalleryView" Height="600" Width="800"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">
    <Border CornerRadius="10" BorderThickness="1">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{StaticResource winBorderColor1}" Offset="0"/>
                <GradientStop Color="{StaticResource winBorderColor2}" Offset="0.5"/>
                <GradientStop Color="{StaticResource winBorderColor3}" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <!-- Main Content -->
        <Grid Grid.Row="1" Background="#1C0E49">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Main image display -->
                <Border Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Image x:Name="MainImage" Stretch="Uniform" Width="400" Height="400"/>
                </Border>
                <!-- Thumbnail list -->
                <Border Grid.Column="1" Padding="5">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ListBox x:Name="ThumbnailList"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ItemsSource="{Binding ImagePaths}"
                                 SelectionChanged="ThumbnailList_SelectionChanged">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="5" 
                                            BorderThickness="2"
                                            BorderBrush="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBoxItem}}, Path=IsSelected, Converter={StaticResource BooleanToSelectionBorderConverter}}">
                                        <Image Source="{Binding}" Width="80" Height="80" Stretch="Uniform"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>